import * as React from 'react'

interface CommentProps{}
interface CommentState{}

export  default class Test extends React.Component<CommentProps, CommentState> {
    public render() {
        return(
            <div>hello</div>
        )
    }
}
